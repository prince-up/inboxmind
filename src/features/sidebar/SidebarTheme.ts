/**
 * Runtime layout values shared by the sidebar engine and its styles.
 */
export const SIDEBAR_THEME = {
  compactViewportWidth: 900,
  hostId: 'inboxmind-sidebar-host',
  panelWidth: 380,
} as const;
